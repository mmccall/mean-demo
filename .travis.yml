language: node_js
node_js:
  - '0.11'
before_script:
  - npm install -g bower grunt-cli mocha istanbul
  - npm install coveralls
  - gem install sass
  - bower install
services: mongodb
script:
    - grunt:coverage